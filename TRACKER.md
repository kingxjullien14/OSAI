# AIOS Polish Tracker — live status board

> **The single source of truth for this polish/enhancement effort.** Check items off here as they land (one commit per batch). Ground truth comes from the 2026-06-11 multi-agent audit ([audit-synthesis.json](audit-synthesis.json), 43 verified defects + roadmap scoreboard); design intent lives in [PLAN-superapp-uiux.md](PLAN-superapp-uiux.md) — its roadmap checkboxes are stale, trust THIS file.

Legend: `[x]` shipped (commit referenced) · `[~]` in progress · `[ ]` open

---

## Wave 1 — correctness & safety (broken things first)

### Shipped
- [x] **Git safety net** — repo re-initialized, baseline snapshot + audit artifact committed (`8dc2cfd`, `73c461e^`)
- [x] **Windows path correctness sweep** (`73c461e`) — new `src/lib/paths.ts` (separator-agnostic basename/dirname/join/normalize/toFileUrl + tests); fixed revealFile/finderRoot rooting at `/`, FileFinder basename-ranking + dir column + recents, FilesPane go-up/root-label/focusDir/git-decoration keys, BrowserPane file-drop URLs + download open/folder, App.tsx pane labels, drive-letter/UNC/space-containing paths now recognized as file targets
- [x] **Money-agents platform rebase + identity scrub** (`2c41346`) — runtime `home_dir` (no more `/Users/firazfhansurie`), paths derived at load + self-heal migration for legacy stored agents, logs under `~/.aios/logs`, forced-green `firaz` branch deleted, all persona seed/run copy neutralized, mirror rooms `firaz-*` → `aios-*`, two-click agent removal (no native `confirm()`), MoneyAgentsPane empty state, `--dangerously-skip-permissions` disclosed in OracleRoster
- [x] **Rust backend correctness** — Windows pty honors `cwd` ("open terminal here" actually opens there), `resolve_bin`/`opencode_bin` read `USERPROFILE` + scoop/npm/.exe/.cmd probes, ripgrep found on Windows (scoop/cargo/choco/PATH), every synthesized engine failure now carries `is_error:true` + the engine's message (codex `turn/failed`, non-retryable `error`, app-server death, opencode crash-vs-clean-exit via exit code)

### Shipped (cont.)
- [x] **Pane drag & drop fixes** (`bb94759`) — no more text selection during pane drags (suppressed + pointer captured); targets hit-tested via `elementFromPoint` so browser panes acquire; `pointercancel` ends stuck drags; swap targets show "release to swap panes" instead of the misleading path-drop overlay
- [x] **Files → terminal / chat drag-drop** (`bb94759`) — FilesPane rows start a pointer-based drag (HTML5 dnd is dead inside the Windows webview): folder → `cd` in terminal / re-root files pane, file → quoted path insert / chat context; cursor ghost chip, Escape cancels; terminal quoting now PowerShell-correct

### Shipped (cont. 2)
- [x] **App-shell batch** (`2a074cd`) — overlay occlusion set complete (FileFinder/GlobalSearch/close-prompt/onboarding/splash); spark launcher → `codexShellCommand()` (no hardcoded model); cast/attach gone from the Windows catalog; appshot gated to macOS + label fixed; "jarvis" → "chat"; drag strip 6px + under-point hit-test; Mission Control empty-space close + accessible card X; collapsed-sidebar floating cluster (palette/sidebar/bell); accent fills on `--color-accent-fg`
- [x] **Chat failure-truth + empty-hero rescue** (`71863d6`) — error text from `ev.result`; stop renders calm (no red card); retry re-spins on startup failure; regenerate uses the visible bubble; defaultAi derived on model pick; chips+ledger bloom on first keystroke; pre-session sends keep the hero (inline status + retry); `.hero-title`/`.helper-line` adopted; anchored hero; `⏎ send · ⇧⏎ newline` cue; dropdowns close on outside-click/Escape with ARIA; action rows reveal on keyboard focus
- [x] **Terminal/editor correctness** (`22f7f23`) — Ctrl+V/Ctrl+Shift+V paste + Ctrl+C-with-selection/Ctrl+Shift+C copy on Windows (Ctrl+C stays SIGINT); exited-Enter scoped to its pane (+ autofocused restart); Ctrl+S saves only the focused editor with an in-flight guard; the text-size slider drives xterm + Monaco live; spawnShell fallback carries cwd
- [x] **Safety batch** (`c4fa535`) — NotesPane two-click delete; Onboarding replay keeps saved engine/model; lush→calm one-time migration; PulsePane + PluginsPane honest states; cast/attach honest Windows fallbacks

### Open (rolls into Wave 2)
- [ ] **Browser-pane occlusion** — address-bar autocomplete + toasts + annotate badge render under the native webview (pane-local fix in BrowserPane)
- [ ] **Palette trust** — disabled commands look identical + silently no-op; danger flag never surfaced; matcher cost cap; "not installed" models could explain why
- [ ] **Whisper endpoint** — hardcoded localhost:9000, fails only AFTER recording; make it a setting + pre-flight check

## Wave 2 — pane polish & UI/UX convergence *(user: "all panes need polishing, UIUX improved by a lot")*

- [x] **CSS substrate fixes** (`~6a7ac8a^`) — transition baseline into `@layer base` (Tailwind transitions work again); `.lift/.press/.btn-glow` carry color easing; new `.overlay-backdrop`/`.toast-in/out`/`.disclose`/`.skeleton`/`.aios-wave-bar` utilities
- [x] **Composer menus un-clipped** (`6a7ac8a`, user-reported) — fixed-position direction-aware dropdowns with internal scroll
- [x] **Windows drag feedback** (`6a7ac8a`, user-reported) — HTML5 draggable mac-only; pointer drag owns the gesture (ghost, live overlays, hover label, instant drop)
- [x] **Design-system convergence, pane by pane** — verified shipped 2026-06-12 (`90c457d`+`904995d` header spec, `61f2029` menus onto `.surface-pop`, `a4f2804` accent discipline, `586fb35` ratchet, `c4fa535` honest states; FilesPane GIT_COLOR now status tokens). *Residual: FilesPane "scanning…" forever state → Wave 4.*
- [x] **Shared primitives** (`086f7cc` ui.tsx PaneEmpty/Skeleton/CopyButton; ratchet guard `ca602bb` in bundleBoundaries.test.ts)
- [x] **Motion choreography** — verified shipped (FLIP `904995d`, grid reflow `75a4b5f`, smooth jump + `.disclose` `965d27d`, block arrival `9a0d401`, pane mount `fade-in-up` in App.tsx, backdrop/toast utilities wired). *Residual crumbs (pane EXIT motion, 1Hz clock soft tick, list staggers audit) → Wave 4.*
- [x] **Homescreen completion** — verified already shipped (see line in remaining-backlog section)
- [x] **Settings cleanup** (`bfcb734` density/text-size real, `f3be886` traps + ARIA). *Residual: close-X stacking audit → Wave 4.*
- [x] **Terminal/editor/voice theming** (`bfcb734` — live accent themes, real waveform, whisperUrl setting, wave keyframe single-sourced). *Residual: whisper pre-flight check → Wave 4.*

## Wave 3 — signature features (the fun part)

- [x] **Focus Spotlight** (`d1743a3`) — Ctrl+./Cmd+. dims every pane but the active one (45%, eased); auto-suspends maximized/single-pane
- [x] **Starter deck on the empty hero** (`d1743a3`) — quiet pills prefill the composer, vanish on first keystroke
- [x] **Chat hero redesign** (`235cf69`, user-reported) — time-aware greeting, accent-gradient title word, ambient aurora blobs, starter deck upgraded to four lift-on-hover icon cards, decluttered helper; dropdowns open toward the larger side + cap height (no more clipped selectors)
- [x] **Windows menu strip removed** (`235cf69`, user-reported) — the native "AIOS Edit Pane" menu is macOS-only now; chords still work via the in-app keydown handler. *Caveat: on Windows, chords don't fire while focus is INSIDE a browser child webview (the menu was the workaround) — click into app chrome first. If that bites, we can add a Rust-side accelerator later.*
- [x] **Workspaces** (`f83998c`) — see remaining-backlog section
- [x] **Activity Glow** (`a1476cf`)
- [x] **Resume rail** (`6525b41`)
- [x] **Run timeline rail** (`904995d`)
- [x] **Living Pet** (`53a0b87`)
- [x] **Repeat-last** (`a1476cf`) + palette MRU "recent" group (CommandPalette.tsx)
- [x] **Soundscape** (`f6799be`)

## Remaining Wave 2 backlog (next session)
- [x] **Usage everywhere** (`90c457d`) — Windows feed fixed (USERPROFILE/curl.exe), sidebar-footer mount, honest claude "no feed" row, codex labeled with its chatgpt-sub provenance; chat strip explains itself
- [x] **Claude statusline hook installed on this machine** — `~/.aios/bin/statusline.ps1` + `statusLine` merged into `~/.claude/settings.json`; writes `~/.aios/state/usage.json` per tick (real 5h/7d light up after the next claude session starts)
- [x] **Transcript de-dup + redundancy** (`9a0d401`) — footer no longer echoes the reply, thinking dedup (+test), single-bucket ledger collapsed, run pill live-only, block arrival motion
- [x] **Run-phase rail** (`904995d`) — think→write→act→done spine on live activity groups
- [x] **Maximize/restore FLIP morph** (`904995d`) — panes expand/shrink instead of teleporting
- [x] **`.pane-header` convergence complete** (`90c457d`+`904995d`) — pulse/plugins/channels/agents/editor/file-viewer/notes all on the shared header spec
- [x] **Usage truth** (`e950b28`+`7cea8bf`) — bogus test snapshot purged; expired windows zero themselves at the source (the "5h 78% resets now" bug); 24h freshness; strip render-guard; honest claude row wording
- [x] **Select-to-attach context snippets** (`7cea8bf`, user-requested) — select reply text → "add as context" chip → snippet pills in the composer, each riding the next send as its own labeled block
- [x] **Design-token ratchet guard** (`ca602bb`) — text-white/text-black/hover-accent/shadow-2xl counts can only go down; new literals fail the suite
- [x] **Dead bento CSS deleted** (`ca602bb`) — .aios-tile/.aios-omni/.aios-idle-glow (~90 lines), the last fully-open P0
- [x] **Shared primitives** (`086f7cc`) — ui.tsx PaneEmpty/Skeleton/CopyButton; adopted in chat, editor (skeleton code-lines), agents, cast fallbacks, bridges
- [x] **BrowserPane occlusion** (`45fc534`) — autocomplete joins the native-hide list; toasts become a reserved strip above the page
- [x] **Interactive surfaces** (`bfcb734`) — xterm/Monaco carets + selection follow the live accent; VoiceButton equalizer; whisperUrl setting; text-size + density tokens finally consumed app-wide; wave keyframe single-sourced
- [x] **Dead code** (`0420a6d`) — hotkeys metadata deleted (reportUsage kept: it feeds Settings diagnostics)
- [x] **Palette trust** (`4cec60d`) — danger markers, dimmed disabled rows, surfaced ok:false failures (toast), matcher cost cap
- [x] **Motion** (`965d27d`) — smooth jump-to-latest, .disclose diff expansion with show-less
- [x] **Activity Glow + repeat-last** (`a1476cf`) — breathing chrome seam on streaming panes; Ctrl+Shift+K re-fires the last command

- [x] **Claude usage live + account-global** (`7e3a953`, user-reported) — OAuth usage endpoint (the /usage panel's own source) replaces the stale statusline file (now fallback); chat strip + sidebar share one global source; verified live (39%/31%)
- [x] **Per-model usage windows** (`4569765`) — sonnet/opus weekly carve-outs (+codex spark) parsed from the OAuth payload into a `models` map; nested rows in sidebar + idle home; "7d N% left" chips on matching model-picker rows
- [x] **A11y: ARIA mirror + focus traps** (`f3be886`) — FileFinder/GlobalSearch get the palette's full dialog+combobox+listbox pattern (activedescendant, live counts, aria-busy) + keyboard parity (Home/End/Page/Ctrl-N/P); shared `trapTab` applied across palette/finder/search/Settings/close-prompt/PinSite/Onboarding/goal-editor; Mission Control gains dialog semantics
- [x] **Grid reflow glides** (`75a4b5f`) — grid-template tracks transition on pane open/close/layout-reset (Chromium interpolation), gated off during gutter drags; gutters end on pointercancel
- [x] **Token ratchet tightened** (`586fb35`) — text-white 6→0 (accent-fg CTAs, themed avatar scrim), shadow-2xl 16→3 (--aios-shadow-pop; floor = locked TerminalComposer)
- [x] **Docs reorg committed** (`6fdfb7f`) — user moved secondary plans/notes into misc/; live docs (TRACKER, PLAN-superapp-uiux, README, WINDOWS*) stay at root

- [x] **Resume rail on the empty hero** (`6525b41`) — last 3 sessions one click away (engine chip + age, "all sessions →" opens the /resume picker); vanishes with the starter deck on first keystroke
- [x] **Pane menus onto `.surface-pop` + honest "not installed"** (`61f2029`) — browser suggest/downloads/bookmarks, cast roster, files project picker converge on the shared popover surface; disabled model rows' tooltips name the missing CLI + the install one-liner
- [x] **Accent-hover discipline** (`a4f2804`) — 30 → 10 per DESIGN.md §6: decoration (rows/chips/floats/secondary buttons) hovers `border-strong`; accent survives only on true primary/add affordances; ratchet pinned at 10 with rationale
- [x] **Homescreen final touches** — verified already shipped (focus-on-mount, engine hint line, weekday greeting, composer-grade command line); stale line removed

- [x] **Workspaces** (`f83998c`) — save/restore/delete named pane layouts from the palette; snapshot = persistable panes + grid fr-fractions; restore rides the shared boot hydration (terminal session reattach, browser last-url), seeds target tracks pre-swap (reflow glides), detaches busy chats instead of killing; a11y naming modal
- [x] **Hero overlays open downward** (`a2e8e7a`, user-reported screenshot) — resume picker / slash / @-mention clipped at the pane top on the hero (composer sits at 14vh); direction now follows the layout
- [x] **Session labels sanitized** (`0d9213f`, user-reported screenshot) — CLI slash turns leaked raw `<command-name>` XML into resume rows/hero rail/repainted transcripts; new leaf `sessionLabel` module + 4 unit tests
- [x] **Workspace chips on the idle launch row** (`2090602`) — saved layouts restore in one click from the home

## UI/UX wave 2 — chat pane + idle dashboard *(user, 2026-06-12: "further improve the chat pane and idle dashboard")* — **COMPLETE**

### Chat pane (all shipped)
- [x] **Hover timestamps** (`065879c`) — send/arrival clock on both bubbles' hover rows; resumed turns carry their REAL transcript times (Rust `ts` on ChatTurn from the JSONL line timestamps), never faked as "now"
- [x] **Day separators** (`065879c`) — quiet hairline + "today / yesterday / wed 11 jun" where the transcript crosses midnight
- [x] **Wide-pane readability** (`21dd939`) — `.chat-col` container query: 42rem reading measure, breathes to 52rem when the PANE is ≥1100px (half-width panes keep their measure)
- [x] **Wrench menu polish** (`21dd939`) — permission rows explain their consequences inline ("runs everything without asking — trusted repos only" etc.); the trigger already had its aria-label
- [x] **Queued-row affordance** (`21dd939`) — queue head says "sends next", the rest "queued #n"; remove-X says what it does
- [x] *(bonus, `065879c`)* USERPROFILE fallback on every bare HOME read in chat.rs — resumed-transcript repaints + the session store were HOME-only and silently dead on Windows GUI launches

### Idle dashboard (all shipped)
- [x] **Git pulse on recent projects** (`21dd939`) — dirty/ahead/behind status dot + branch tooltip per row (data was already polled, only a footer count before)
- [x] **Command-line seed recall** (`21dd939`) — ↑ in the empty line recalls the last seed; hint line says so
- [x] **Living pet wiring** (`53a0b87`) — momentary reactions over the metabolic mood: user send → attentive, finished run → celebrate, error/failed turn → wince; plays in the pet pane AND the idle-home companion (reduce-motion safe)
- [x] **Soundscape** (`f6799be`) — opt-in (default OFF) whisper-quiet synthesized cues on run done/fail; zero assets; toggle in Settings → notifications

### Parked
- [ ] poll-swap transitions on cast/mirror rosters (macOS-gated panes — low priority on the Windows daily driver)
- [ ] ratchet floors if ever unlockable: accent-hover 10 (all true primaries), shadow-2xl 3 (locked TerminalComposer)

## Wave 5 — Signature Styling *(planned 2026-06-13 — full spec + component map in [PLAN-wave5-styling.md](PLAN-wave5-styling.md))*

> Restyle with Framer Motion (`motion`) + vendored/token-adapted Magic UI · Aceternity · ReactBits components. Hard rules: ratchet stays green (enforces tokenization), reduce-motion bridged into MotionConfig + every canvas fx, accent discipline survives the gradient temptation, no fx under native webviews or in the locked TerminalComposer, +60kb gz cap for the wave.

- [x] **W5-1 Foundation — complete** (this commit): `motion` v12 + clsx + tailwind-merge installed; `src/components/fx/` is live — `cn.ts` (the vendoring contract's cn()), `motionTokens.ts` (reads `--aios-dur-*`/`--aios-ease-*` off :root once; exports overlayFade/modalPop/toastPop/paneExit, each reading reduce-motion at call time), `reducedMotion.ts` (one truth: html dataset + OS query, reactive hook via useSyncExternalStore on the settings store), `MotionRoot.tsx` (LazyMotion domAnimation **strict** + MotionConfig reducedMotion bridged — mounted around `<App/>` in main.tsx; W5-4 swaps the feature bundle to domMax for layoutId). **AnimatePresence migration**: all 8 overlays (palette incl. the WAAPI morph via new `peekPaletteMorphSource()` suppressing the motion entrance for that one mount, finder, search, ShortcutHud, Settings, Mission Control, pin-site, save-workspace — whose sticky-draft ref died: AP replays the last open render) + the busy-chat close-prompt; **pane exit** = AP `popLayout` around the grid children (PaneCard root is an `m.div` forwarding the React-19 ref-as-prop so the pop can measure; closingKeys/closingPanesRef/setTimeout(170)/double-close guard all deleted; survivors still glide on the CSS grid-template transition); **toasts** = toastPop() at all 5 sites (App flash — timer-race plumbing simplified to one timer, conductor pill, Bridges, AppCast, Browser — the -translate-x-1/2 classes dropped since motion owns x:-50% across every keyframe). **Retired CSS deleted**: aios-overlay-out, aios-modal-out, aios-toast-in/out, aios-pane-out, every `[data-closing]` rule, `.toast-in/.toast-out`, `.pane-exit`; `.modal-in`/`.overlay-backdrop` remain entrance-only for PetPane bubble + RunCinema (their waves come later). useExitState + ExitGate deleted from ui.tsx. Settings mounts on first open and stays (lazy chunk still on-demand). Gates: tsc 0 · 99/99 · build green. **Bundle: main 142.16→171.12 (+28.96), css 23.13→22.98 (−0.15), lazy chunks ±0.04 → wave spend +28.8kb gz of the 60kb cap (~31kb headroom for W5-2..5 incl. the domMax swap)** — the motion runtime is the whole cost; clsx/tailwind-merge tree-shake to 0 until W5-2 imports cn().
- [x] **W5-2 Idle showcase — complete** (this commit): seven fx primitives vendored + token-adapted into `src/components/fx/`, all reduce-motion gated. **CommandLine** got Aceternity's placeholders-and-vanish split across two hooks — `useRotatingPlaceholder` (carousel overlaid as an AnimatePresence `m.span`, native placeholder blanked, holds phrase 0 under reduce-motion) + `useVanish` (canvas dissolve: rasterizes the typed text at the input's real font + resolved `--color-text`, samples glyph pixels to particles, disperses up-right ~560ms; resolves instantly under reduce-motion). OUR chrome fully preserved — intent chips, ↑ recall, `$`/`/` routing, FLIP morph all intact; the chat seeds immediately while the vanish is pure eye-candy over the emptied input. **Backdrop**: `Spotlight` (one-shot accent wash sweeping in from the top, settles, static radial under reduce-motion) + `DotPattern` (masked `--color-border` SVG grid, no animation) layered behind the existing liveness drift blobs. **Greeting**: `BlurText` per-word blur-rise (keyed by part+name so it replays once on a time-of-day flip, not the 30s tick); the gradient name word now also slow-pans (`aios-greet-shimmer`, accent→highlight, 8s, frozen under reduce-motion). **NumberTicker** (spring-to-value, `tabular-nums`, jumps under reduce-motion) on the streak count + every usage % (in the shared UsageGlance UsageBar/ModelRows → benefits the sidebar too). **Spotlight rows**: `spotlightMove` + `.aios-spotlight` CSS (low-alpha `--color-text` radial, NOT accent — neutral surfaces per §6) on recent-project rows, quick actions, workspace + pinned chips. Gates: tsc 0 · 99/99 (ratchet green — every vendored file tokenized, 0 hex/text-white/shadow-2xl) · build green. **Bundle: main 171.12→182.80 (+11.68 gz, mostly tailwind-merge now that cn() is imported), css +0.15 → cumulative wave spend ≈ +40.6kb of the 60kb cap (~19kb headroom; W5-4's domAnimation→domMax swap must be measured against it).**
- [x] **W5-3 Chat — complete** (this commit): five more fx primitives, all reduce-motion gated. **BorderBeam** (Magic UI offset-path beam, padding-box∩border-box mask incl. -webkit- for WebKit, token gradient accent→highlight) is the Activity Glow upgrade — it laps the busy pane's border (App.tsx PaneCard, `busy && !maximized`, z-30, replacing the breathing seam) AND the Conductor pill; reduce-motion → a static accent ring. **SplitText** per-word spring rise on the chat empty-hero title (keyed fresh/resume so it fires once, gradient "work" word preserved as a unit). Starter deck wrapped in **TiltCard** (±4° pointer tilt + `.aios-spotlight` glare; `lift` dropped to avoid double transform). Send CTA = **Magnet** (4px cursor pull) + `.aios-shimmer` (hover-only light sweep, App.css; idle shimmer banned per §6) on the one accent button. **BlurFade** (10px rise + 4px blur, forwards ref) replaces the CSS fade-in-up on settled transcript blocks (user/approval/result); streaming kinds stay plain divs so token appends never re-trigger. Notification panel → **AnimatedList**: AnimatePresence + spring-in-at-top + slide-out, with `layout` added now (no-ops under domAnimation, activates on the W5-4 domMax swap); the capped CSS `.stagger` retired there. Gates: tsc 0 · 99/99 (ratchet green) · build green. **Bundle: main 182.80→183.33 (+0.53), ChatPane 37.92→38.66 (+0.74), css +0.06 → cumulative wave ≈ +41.2kb of the 60kb cap (~18.8kb headroom; the W5-4 domAnimation→domMax swap is the next big spend).**
- [ ] **W5-4 Shell** — Dock magnify on the icons-only rail, layoutId sliding indicators (Settings nav + palette selection), motion-variants staggers (Mission Control, palette groups), Mission Control card glare, onboarding step slides + HoverBorderGradient on lone primaries
- [ ] **W5-5 Personality + audit** — ClickSpark on `.press`, pet confetti on long-clean-run (rides bubble rate-limits), liveness Ripple behind the companion — all behind a new `funFx` setting; full audit: bundle ≤ +60kb gz, ratchet green, both themes, reduce-motion sweep, 60fps with a browser pane casting. Rejected-by-design list documented (meteors/marquee/globe/splash-cursor)

## Wave 4 — deep polish, QoL, wiring truth & the fun tier *(planned 2026-06-12 — full spec in [PLAN-wave4.md](PLAN-wave4.md))*

> Original plan verified ~95% shipped (tsc clean · 91/91 tests · ratchets pinned). Wave 4 = the residuals + the next layer. Sessions below mirror the plan's roadmap; check off here as batches land.

- [x] **W4-1 Truth — wiring audit complete** (this commit) — per-item verdicts:
  - **A1 whisper pre-flight FIXED** — `whisperReachable()` probe (1.5s budget, 60s ok-TTL, failures never cached) runs before `getUserMedia`; ChatPane mic errors surface in a dismissible composer strip (start AND stop were silently swallowed); `whisperUrl` finally has a Settings → general row (the setting existed but was uneditable). TerminalComposer is locked and still swallows — its mic now fails fast pre-recording, which is the win that matters.
  - **A2 Windows gating FIXED** — `isWindows` added to platform.ts (distinct from `!isApple`: Linux keeps tmux/launchd surfaces); OracleRoster hides the doomed one-tap "spawn my oracle" on Windows (both layouts) + one honest line; BridgesPane hides "pair personal" (mac wwebjs script) + honest "detected on the macOS bridge host" note. Monitor needs no gate: only reachable from tmux/oracle panes, which can't exist on Windows.
  - **A3 primary oracle ID FIXED** — `primaryOracleId` setting (default "firaz" preserves WhatsApp routing) + Settings → oracles row; OracleRoster reads it everywhere (spawn/guard/warning copy). Rust side already had the `AIOS_PRIMARY_ORACLE` env override.
  - **A4 notification deep-links VERIFIED FINE** — exactly 2 live emitters (chat.done App.tsx, chat.needs_input ChatPane), both carry reattach-capable chat targets; dispatcher handles every declared target type incl. closed-pane reattach. pane/terminal/file/url/diagnostics targets are forward-built (handlers exist, no emitters yet).
  - **A5 pet↔terminal bus FIXED (real gap)** — TerminalRuntime had ZERO pet wiring. Now: ctx%-change → `onPetUsage({pct: 100-ctxPct})` (2s throttle), spawn failure → `onPetError` (pty-exit carries no exit code, so a clean `exit` must NOT wince — spawn-fail is the one definite error), composerSend → `onPetUserMessage({textLength})`.
  - **A6 usage feeds VERIFIED FINE** — degradation chain is honest end-to-end: OAuth live → statusline file (>3h snapshots dropped at source) → nulls → explicit "no recent usage report" row + tooltip; expired windows zero at source (e950b28). Freshness stamp unnecessary: nothing stale can render.
  - **A7 workspace restore VERIFIED FINE** — dead pty → tmux reattach → fresh-shell fallback → inline error; browser url: live → memKey → persisted triple fallback; busy chats `detach(true)` (background + notify); mismatched saved tracks → silent default-tracks fallback.
  - **A8 Settings close-X FIXED** — the floating X occluded embedded Channels/Plugins header buttons (refresh / pair); `[&_.pane-header]:pr-12` on the full-bleed wrapper clears them.
  - **A9 FilesPane "scanning…" FIXED** — explicit scan state: shimmer skeleton while scanning, "couldn't scan" vs "no projects found" + rescan button after (a failed/empty first scan used to show "scanning…" forever and never retry).
  - **A10 hex stragglers FIXED** — 4 dead `#22c55e` fallbacks → `var(--color-success)`; NEW ratchet pins hex at 0 in components (exempt: TerminalRuntime ANSI palette, Settings swatch definitions).
  - **B7 maximize hint** — restore tooltip now says "(Esc)"; the auto-fading helper-line stays in W4-2 motion. **B9 danger sweep VERIFIED** — notes/agents/oracle two-click ✓; workspace delete danger-marked + recoverable ✓; bookmark removal one-click by browser convention ✓.
- [x] **W4-2 Feel — complete** (this commit) — per-item:
  - **Overlay EXIT motion (B10)** — found the smoking gun: `.overlay-backdrop[data-closing]` exit CSS existed since wave 2 but NOTHING ever set data-closing. New `useExitState` hook (ui.tsx) + `aios-modal-out` keyframe; wired into CommandPalette, FileFinder, GlobalSearch, Settings (ExitGate keeps the lazy parent mount alive through the exit), Mission Control, PinSiteModal, SaveWorkspaceModal (sticky-draft so the input doesn't blank mid-exit). Close-prompt keeps instant dismiss by design (decision modal) but its backdrop now fades in + token shadow.
  - **Pane close exit (B1)** — `closePane` plays `.pane-exit` (fade+scale 170ms, pointer-events off, double-close guarded) before removal; grid reflow glides survivors; reduce-motion removes immediately.
  - **Clock soft tick (B2)** — keyed `.clock-tick` glyphs (150ms opacity ease, tabular-nums = zero shift).
  - **Stagger sweep (B3)** — Mission Control cards, notifications panel, FilesPane listing (rows keyed by path so only NEW entries animate).
  - **Maximize hint (B7)** — CSS-only "esc to restore" settle-hold-fade pill (keyed per maximize; native-webview panes may occlude it — known platform limit).
  - **Toast unification (B6)** — `.toast-in/out` had ZERO adopters. App flash toast was `.modal-in` (a modal's gesture) + shadow-2xl + no exit + a race where flash #1's timer cleared flash #2 early — all fixed (timer refs, leaving beat, keyed re-entry). BridgesPane toast got entrance/exit + token shadow; AppCast toast + Browser annotate badge joined `.toast-in`.
  - **Gutters (B11)** — resting pip at opacity-30 (was invisible until hover), double-click equalizes the two tracks (persisted; reflow glides).
  - **Sidebar home anchor (B12)** — `aios` wordmark + Home glyph above the OPEN list; one click rests all panes to the idle home (they stay restorable); icons-only mode shows the glyph.
  - **PaneEmpty adoption (B5)** — NotesPane empty gains a "new note" pill; BridgesPane "no channels detected." → PaneEmpty with platform-aware hint + refresh action. FileViewer/Editor have no bare empties (always open with a file); MoneyAgents/AppCast already adopted.
  - **Scroll restore (B8) VERIFIED FINE** — hidden panes are `display:none` (DOM alive, scroll intact); detached-chat reattach lands at bottom by design (you return for the result).
  - **Light theme (B4)** — static audit: zero `bg-white/` washes, zero text-white/black (ratchet), `glass` is filter-only; fixed the real finds: MoneyAgents log `bg-black/25` → `bg-[var(--color-bg)]/60`, Mission Control card `shadow-2xl shadow-black/50` → shadow-pop, PinSite + close-prompt shadow-2xl → shadow-pop, 2 stray `shadow-lg` toasts → token. *A human visual pass in light theme remains worthwhile; nothing objectively broken found.*
  - **PaletteShell extraction (B13) — deliberately dropped**: the three shells now share matcher, ARIA, traps, nav, exit hook, and identical chrome strings; extracting the remaining ~20 lines of JSX each would add an indirection layer for near-zero drift risk. Revisit only if a fourth palette appears.
- [x] **W4-3 Flow (chat QoL) — complete** (this commit):
  - **Find-in-chat (Ctrl+F)** — App's context-aware ⌘F router gains a chat branch (`aios-chat-find`, same pattern as the browser; before this, ⌘F on a chat pane **fullscreened it**). `.surface-pop` bar top-right: live count, Enter/Shift+Enter cycle, Esc back to composer. Block-granular matching over a `blockSearchText` corpus that includes collapsed thinking + tool args/output (text native find can never reach); hits force-open their ThinkingBlock/ActivityGroup via a new `forceOpen` prop; the current hit scroll-centers with a one-shot `--color-highlight` wash (`.find-current`, reduce-motion safe).
  - **Conversation minimap** — chats ≥9 blocks get a tick rail (jump-pill clearance convention): user=accent, approval=warning, failed result=danger, assistant=neutral, thinking/activity=faint; click jumps. Geometry from the same per-block ref map as find (one rAF batches all offsetTop reads per blocks change).
  - **Per-turn token sparkline** — last ≤24 result-turn token counts as a 56×14 faint polyline + accent latest-dot, sharing the footer row with the ctx readout (row now renders when either exists).
- [x] **W4-4 Reach (window manager) — complete** (this commit):
  - **Snap-to-zone pane dragging** — the drag gesture now reads position within the hovered pane: left/right ~30% = INSERT before/after (new `placePane` splices the order; the grid-template transition glides everything), middle = the original swap. Zone overlay is honest per §6: accent edge bar + washed half only on the active insertion edge, label always says exactly what release does ("place before / place after / swap panes"). Arrow buttons stay as the keyboard fallback.
  - **"Pick up where you left off" pill** — idle hero, between clock and command line: when every pane is hidden (the sidebar home anchor's state) it un-hides all; on a fresh boot with reopenLastLayout off it rehydrates the stored layout. Hides itself when there's nothing to resume; pane labels in the tooltip. (Close-all wipes the stored layout by design — verified, so no stale offers.)
  - **Type-ahead intent chips on the command line** — `$build` → "run in a terminal" (rides the established `{type:"shell",cmd}` shape), `/` → palette, plain text → up to 2 matching recent projects. Chips not interception: Enter keeps seeding a chat EXCEPT explicit `$`/`/` prefixes; Tab (badged on the top chip) or click takes the intent. Hint line documents `$`.
- [x] **W4-5 Palette — complete** (this commit):
  - **`>` action mode** — the palette becomes a verb router: `>theme dark` / `>accent blue` **live-preview as you arrow** (baseline refs hold the pre-verb value; Enter/click commits, leaving the verb or closing uncommitted reverts), `>workspace <name>` scopes to saved layouts, bare `>` lists the verbs (picking one refines the query in place instead of closing). Footer hints `> verbs`.
  - **Preview detail strip** — `preview?: string[]` threaded through AiosCommand → palette Command; selected resume rows show engine + age + cwd (mtime is unix SECONDS — caught a would-be "28000d ago" bug), project rows show root + kind, workspace rows show their pane labels. Renders as a compact strip above the footer ONLY when the row carries lines — deliberately not the Raycast right-rail (constant-width, no per-arrow layout jumps; revisit if previews grow richer).
  - **Mod+? shortcut HUD** — new `lib/shortcuts.ts` is the ONE catalog (launch/panes/app groups, platform-correct keycaps, context notes); `ShortcutHud` overlay (exit-motion, stagger, trap) binds to mod+?; **Settings cheat-sheet now renders the same catalog** — the hand-maintained 6-of-~18 array is deleted, so the two can never drift.
  - **Approval echo** — the resolved approval card keeps a mid-ellipsized echo of what was approved (full on hover, one-click copy); the transcript is auditable again.
- [x] **W4-6 Fun — complete** (this commit):
  - **Run Cinema** — new `RunCinema.tsx`: any finished activity group grows a hover-revealed "▶ replay" that re-streams the captured run-event timeline (reasoning beats, tool calls + outputs, permission asks, completion stats) at director's pace. Real timestamps drive pacing with gaps clamped 36ms–1.4s (dead air skips, bursts stay legible); transport = play/pause (space), 1×/4×/16× speed, scrubber, elapsed/total clock; message deltas coalesce into one growing reply (per-frame rows would be confetti); segment mapping replays from the k-th run's start. Pane-scoped overlay, Esc closes, reduce-motion plays near-instant.
  - **Pet companion bubbles** — pet.ts gains a rate-limited bubble bus (per-kind cooldowns: ctx-low 15min, failed-run 10min, long-clean-finish 20min — charming, never Clippy). Speech chip renders over the sprite in BOTH the pet pane and the idle-home companion. Text-only v1 (the /handoff action button stays parked).
  - **Live pulse backdrop** — the idle drift blobs now breathe with real state: liveness = agents running + dirty repos + unread, driving wrapper opacity 0.55→1.0 over a 2s ease. Opacity-only (composite-cheap); reduce-motion already freezes the drift.
  - **Command-line → palette morph** — new `paletteMorph.ts` (consume-once rect handoff, 1s staleness guard): an empty submit on the idle command line records its rect; the palette's next mount plays a WAAPI inverted-transform from that surface into place (260ms spring, suppresses .modal-in for that one open). Normal ⌘K opens keep the standard entrance; reduce-motion skips.
- [x] **W4-7 Autonomy — complete** (this commit):
  - **Scheduled agents** — found a working-but-silent inline scheduler already in App (the W4 plan's audit missed it: it inlined its own cadence parser instead of consuming the dangling `loadMoneyAgentLastScheduledRun`). Upgraded in place rather than duplicated: cadences now parse via the new lib `scheduleIntervalMs` (hourly/daily/weekly + "every N min|hours|days" with a 5-minute quota floor + the legacy "always"→6h / "work block"→daily phrasings kept), due-math via `isMoneyAgentDue`/`dueMoneyAgents` (never-stamped fires immediately — the user asked for autonomy), stamp-before-fire stays. The tick now routes through `openMoneyAgentChat` (which already backgrounds command-spawns — ~50 lines of duplicated spawn logic deleted) and returns the pane key so each pulse pushes a **deep-linked notification** ("scheduled pulse: X — click to watch" → focusPane un-hides the background pane). Create-form schedule field documents the accepted cadences. +2 unit tests on the pure seams (parser + due-math), 93 total.
  - **Smart starter deck** — the empty hero's four cards now read the workspace via one shallow readDir: a manifest (package.json/cargo/pyproject/go.mod) makes "explore" name the project + start from the manifest and turns "fix" into "run the tests, fix the first failure"; a git repo turns "plan" into "catch up — what changed lately?"; a README-only folder summarizes it; an empty folder offers "start from scratch". Probe failure or no cwd → the static deck, unchanged.
- [x] **W4-8a Conductor — complete** (this commit): mod+shift+J toggles push-to-talk (the W4-1 whisper pre-flight means a dead server fails BEFORE recording; plain mod+J stays focused-pane dictation). New `lib/conductor.ts` — a PURE intent parser (6 unit tests, 99 total): segments split on then/and/commas; grammar = spawn panes ("open a terminal", "browser on github.com" → url), `run <verbatim>` (no command magic), `ask claude to …` (a repair pass folds "and …" tails back in so prompts don't shed clauses), `switch to <name> mode` (only if the workspace exists — no false applies), theme, go-home. Execution cascades 160ms/step over existing primitives (spawn/applyWorkspace/setTheme/hide-all) — you watch your words build the layout; a plan that parses to nothing seeds a chat with the whole transcript instead of dying. Listening pill top-center (Esc cancels, intercepted before maximize-Esc); catalogued in shortcuts.ts (HUD + cheat-sheet). Per guardrail 3, nothing touches the model's context — routing is plain code.
- [x] **W4-8b Windows capture backend — CODE COMPLETE, awaiting live run-verify** (this commit): new `src-tauri/src/wincast.rs` (~450 lines) implements the full `appcast_*` surface on Windows.Graphics.Capture behind the SAME command names — AppCastPane needed zero rewrite, exactly as the plan promised. Architecture mirrors the mac module 1:1: EnumWindows picker (visible/titled/non-tool/non-cloaked/≥80×60, exe basename as app name, skips AIOS itself), GraphicsCaptureItem-from-HWND interop → free-threaded Direct3D11CaptureFramePool → FrameArrived copies the GPU texture into a child-HWND swapchain (DXGI_SCALING_STRETCH so slot-resize and source-resize can't tear; pool+backbuffer re-arm on source resize). Child HWND answers WM_NCHITTEST with HTTRANSPARENT (the `ignoresMouseEvents` twin — phase A is display-only); ALL HWND ops marshal to the main thread (create/move/show/destroy via run_on_main_thread + channel); parent HWND comes from raw-window-handle so tauri's internal `windows`-crate version never has to unify with ours. Three real compile battles, all documented in-code: windows-core 0.61 interfaces are NOT Send (justified `SendWrap` — D3D11/DXGI free-threaded contract + agile WinRT classes + one GPU mutex), Rust 2021 precise-capture sneaking the bare field past the wrapper (accessor method), and the rwh dep. Frontend: catalog entry + pane gate ungated for Windows, picker empty-copy platform-aware, the Screen-Recording TCC rescue UI correctly stays mac-only (Graphics.Capture needs no grant). Gates: cargo check CLEAN (0 wincast warnings) · tsc 0 · 99/99 · build green. **RUNTIME VERIFIED 2026-06-13** after one live-run fix: attempt #1 showed an empty pane (capture started, frames presented, but the mirror child HWND composited UNDER the WebView2 subtree — Chromium spawns sibling intermediate-D3D windows after ours, and set_bounds used SWP_NOZORDER so we never re-won). Fix `24cc3ce`: SetWindowPos(HWND_TOP) at create + every bounds tick, `[wincast]` eprintln breadcrumbs (child rect / capture started / first frame / Present errors), and the resize path only adopts a new size when pool.Recreate AND ResizeBuffers both succeed. User-confirmed: frames render + track the pane. Display-only is the designed phase-A scope; input forwarding = a future phase B.

## Verification gates (every batch)
`npx tsc --noEmit` · `npm run test:chatpane` · `cargo check` (Rust batches) · `npm run build` (per wave) · manual run at the end

---
*Maintained by the polish session(s). When an item ships, move it to the wave's "Shipped" list with its commit hash.*
