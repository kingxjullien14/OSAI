# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri build artifacts
src-tauri/target

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Dependencies & build output
/node_modules/
node_modules/
/dist/
src-tauri/target/

# OS / editor cruft
.DS_Store
*.dmg

# Local env
.env
.env.*

# Windows port: local screenshots + secrets (never commit)
windows-*.png
*.key
src-tauri/.motion.key

# Updater release manifest — a per-release artifact (regenerated + uploaded to
# GitHub Releases by scripts/make-latest-json.ps1), never source.
latest.json

# agent worktrees + local session state
.claude/
scripts\aios-watch.log
